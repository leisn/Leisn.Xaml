<Window x:Class="WpfDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ln="https://leisn.com/xaml/wpf"
        xmlns:local="clr-namespace:WpfDemo"
        mc:Ignorable="d" xmlns:sys="clr-namespace:System;assembly=mscorlib"
        Title="MainWindow" Height="450" Width="800"
    xmlns:theme="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2">
  <Window.Resources>

  </Window.Resources>
  <Grid Margin="10">
    <Grid.ColumnDefinitions>
      <ColumnDefinition/>
      <ColumnDefinition/>
      <ColumnDefinition/>
    </Grid.ColumnDefinitions>
    <ln:StackPanel Spacing="15" HorizontalAlignment="Center" VerticalAlignment="Center">
      <ComboBox x:Name="langsBox" Width="{Binding ElementName=rangbox,Path=ActualWidth,Converter={StaticResource NumberOperateConverter}, ConverterParameter=-20}"/>
      <ln:RangeBox x:Name="rangbox" Minimum="-1" Title="RangeBox" Width="200"/>
      <ComboBox IsEditable="True">
        <ComboBox.ItemsSource>
          <x:Array Type="sys:String">
            <sys:String>ssss</sys:String>
            <sys:String>1111</sys:String>
            <sys:String>2222</sys:String>
            <sys:String>3333</sys:String>
          </x:Array>
        </ComboBox.ItemsSource>
      </ComboBox>
      <Button Content="{ln:Lang Ask}" Click="Button_Click" HorizontalAlignment="Center" Height="22" Style="{StaticResource ButtonPrimaryStyle}"/>
      <Button Content="{ln:Lang ValueFormat,{Binding ElementName=rangbox,Path=Value}}"  HorizontalAlignment="Center" Height="22"/>
    </ln:StackPanel>
    <ln:PropertyGrid Grid.Column="1" x:Name="perpertyGrid"/>
    <Grid Grid.Column="2">
      <Grid.RowDefinitions>
        <RowDefinition/>
        <RowDefinition Height="20"/>
        <RowDefinition/>
      </Grid.RowDefinitions>
      <StackPanel Margin="10,0">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
          </Grid.RowDefinitions>

          <Border x:Name="ColorBlock" Height="{Binding RelativeSource={RelativeSource Self},Path=ActualWidth}"
                  BorderThickness="1" BorderBrush="{DynamicResource BorderLighterBrush}">
            <Border.Background>
              <SolidColorBrush Color="{Binding ElementName=colorCircle,Path=SelectedColor}"/>
            </Border.Background>
            <Grid>
              <Border>
                <Border.Background>
                  <LinearGradientBrush EndPoint="0,0.5" StartPoint="1,0.5">
                    <GradientStop Color="White" Offset="1" />
                    <GradientStop Color="#80ffffff" Offset=".5" />
                    <GradientStop Color="#00ffffff" />
                  </LinearGradientBrush>
                </Border.Background>
              </Border>
              <Border>
                <Border.Background>
                  <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="Black" Offset="1" />
                    <GradientStop Color="#80000000" Offset=".5" />
                    <GradientStop Color="#00000000" />
                  </LinearGradientBrush>
                </Border.Background>
              </Border>
            </Grid>
          </Border>
          <Slider Grid.Row="1" Orientation="Horizontal" Margin="0,5,0,0"
                Width="{Binding ElementName=ColorBlock,Path=ActualHeight}"
                Minimum="0" Maximum="1785" MinWidth="{DynamicResource InputMinHeight}"
                SmallChange="1" LargeChange="10" >
            <Slider.Background>
              <LinearGradientBrush>
                <GradientStop Color="#ff0000" />
                <GradientStop Color="#ff00ff" Offset="0.167" />
                <GradientStop Color="#0000ff" Offset="0.334" />
                <GradientStop Color="#00ffff" Offset="0.501" />
                <GradientStop Color="#00ff00" Offset="0.668" />
                <GradientStop Color="#ffff00" Offset="0.835" />
                <GradientStop Color="#ff0000" Offset="1" />
              </LinearGradientBrush>
            </Slider.Background>
          </Slider>
        </Grid>
      </StackPanel>

      <Grid Grid.Row="2" Margin="10,0">
        <ln:ColorCircle x:Name="colorCircle" />
        <Ellipse Width="50" Height="50" HorizontalAlignment="Center" VerticalAlignment="Center">
          <Ellipse.Fill>
            <SolidColorBrush Color="{Binding ElementName=colorCircle,Path=SelectedColor}"/>
          </Ellipse.Fill>
        </Ellipse>
      </Grid>
    </Grid>
  </Grid>
</Window>
